apiVersion: 1

providers:
  - name: Node Monitoring Dashboard
    folder: Services
    type: file
    options:
      path: /var/lib/grafana/dashboards
        # <path to dashboard definitions> # Default path for Linux is: /var/lib/grafana/dashboards